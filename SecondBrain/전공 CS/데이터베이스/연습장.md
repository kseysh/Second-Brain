```sql
create physics_fall_2009(course_id, sec_id, building, room_number) as (
	select course_id, sec_id, building, room_number
	from section s, course c
	where c.course_id = s.course_id and c.dept_name = 'Phys'
	
)
```

```sql
update student s
set tot_cred = (
	select case 
		when sum(credits) is not null then sum(credits)
		else 0
	from courses natural join takes
	where takes.grade <> 'F' and takes.grade is not null
)
```



















## ㅇㄹㄲㄹ
```sql
with max_budget (value) as
	(
	select max(budget)
	from department
	)
```

```sql
delete from instructor
where dept_name= 'Finance';
```

```sql
insert into course
values ('CS-437', 'Database Systems', 'Comp. Sci.', 4);
```

```sql
insert into course (course_id, title, dept_name, credits)
values ('CS-437', 'Database Systems', 'Comp. Sci.', 4);
```

```sql
update instructor
set salary = case
	when salary <= 100000 then salary * 1.05
	else salary * 1.03
	end
```