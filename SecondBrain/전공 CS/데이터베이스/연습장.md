```sql
select distinct I.name
from instructor as I, student as S, advisor as A
where A.s_id = S.ID and A.i_id = I.ID and not exists (
	select s.course_id, s.sec_id, s.semester, s.year
	from section s
	where s.year = 2021 and s.semester = 'Winter'
	except
	select t.course_id, t.sec_id, t.semester, t.year
	from takes t
	where t.ID = s.ID
)
```

```sql
select 
```