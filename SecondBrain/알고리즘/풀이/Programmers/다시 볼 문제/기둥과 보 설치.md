# 나의 풀이 (틀림)
```cpp
#include <string>
#include <vector>

#define MAX_N 100
#define COLUMN 0
#define BEAM 1
#define DELETE 0
#define INSTALL 1

using namespace std;

bool beams[MAX_N + 5][MAX_N + 5];
bool columns[MAX_N + 5][MAX_N + 5];
int n;

bool checkWalls(){
    for(int y = 0; y <= n; y++){
        for(int x = 0; x <= n; x++){
            if(columns[y][x]){
                // 기둥은 바닥위에 있으면 통과
                if(y == 0) continue;  
                // 다른 기둥 위에 있거나
                if(columns[y - 1][x]) continue;
                // 보의 한쪽 끝 부분위에 있어야 한다
                if(beams[y][x]) continue;
                if(x > 0 && beams[y][x - 1]) continue;    
                
                return false;
            }
            if(beams[y][x]){
                if(y == 0) return false; // 바닥에 보를 설치하지 않는다.
                // 보는 한쪽 끝 부분이 기둥 위에 있거나
                if(columns[y - 1][x]) continue;  
                if(columns[y - 1][x+1]) continue;
                
                // 양쪽 끝 부분이 다른 보와 동시에 연결되어 있어야 한다.
                if(x == 0) return false;
                if(beams[y][x-1] && beams[y][x+1]) continue;    
                
                return false; // 조건 불만족시 false
            }
        }
    }
    return true;
}

vector<vector<int>> solution(int nn, vector<vector<int>> build_frame) {
    n = nn;
    
    for (int i = 0; i <= n; i++) {
        for (int j = 0; j <= n; j++) {
            beams[i][j] = false;
            columns[i][j] = false;
        }
    }
    
    for(const vector<int> &v : build_frame){
        int x = v[0];
        int y = v[1];
        // if(!inRange(y, x)) continue; => inRange 검사 안함
        int type = v[2];
        int action = v[3];

        if(type == BEAM){
            if(action == INSTALL){
                beams[y][x] = true;
                if(!checkWalls()) beams[y][x] = false;
            }
            else {
                beams[y][x] = false;
                if(!checkWalls()) beams[y][x] = true;
            }
        }else{
            if(action == INSTALL){
                columns[y][x] = true;
                if(!checkWalls()) columns[y][x] = false;
            }
            else {
                columns[y][x] = false;
                if(!checkWalls()) columns[y][x] = true;
            }
        }   
    }
    
    vector<vector<int>> answer;
    
    for(int x = 0; x <= n; x++){
        for(int y = 0; y <= n; y++){
            if(columns[y][x])answer.push_back({x, y, COLUMN});
            if(beams[y][x]) answer.push_back({x, y, BEAM});
        }
    }
    
    return answer;
}
```
2시간 가량 뭐가 틀렸는지 고민했는데, 문제는 continue;
continue를 사용함으로써 기둥을 검사한 후 보를 검사하는 코드를 건너뛰어버렸다.
정답 코드처럼 기둥과 보를 각각 검사할 수 있도록 해야했다.
앞으로 잘 기억하자;;


# 정답 코드
```cpp
#include <string>
#include <vector>

#define COLUMN 0
#define BEAM 1
#define DELETE 0
#define INSTALL 1

using namespace std;

bool beams[105][105];
bool columns[105][105];
int n;

// 기둥이 유효한지 체크
bool isValidColumn(int x, int y) {
    // 바닥 위에 있는 경우
    if (y == 0) return true;
    
    // 다른 기둥 위에 있는 경우
    if (columns[x][y-1]) return true;
    
    // 보의 한쪽 끝 위에 있는 경우
    if (beams[x][y]) return true;
    if (x > 0 && beams[x-1][y]) return true;
    
    return false;
}

// 보가 유효한지 체크
bool isValidBeam(int x, int y) {
    if(y == 0) return false;
    // 한쪽 끝이 기둥 위에 있는 경우
    if (columns[x][y-1]) return true;
    if (columns[x+1][y-1]) return true;
    
    // 양쪽 끝이 다른 보와 연결된 경우
    if(x == 0) return false;
    if (beams[x-1][y] && beams[x+1][y]) return true;
    
    return false;
}

// 전체 구조물이 유효한지 체크
bool checkAll() {
    for (int x = 0; x <= n; x++) {
        for (int y = 0; y <= n; y++) {
            if (columns[x][y] && !isValidColumn(x, y)) return false;
            if (beams[x][y] && !isValidBeam(x, y)) return false;
        }
    }
    return true;
}

vector<vector<int>> solution(int nn, vector<vector<int>> build_frame) {
    n = nn;
    
    // 모든 구조물 초기화
    for (int i = 0; i <= n; i++) {
        for (int j = 0; j <= n; j++) {
            beams[i][j] = false;
            columns[i][j] = false;
        }
    }
    
    for (const vector<int>& cmd : build_frame) {
        int x = cmd[0];
        int y = cmd[1];
        int type = cmd[2];
        int action = cmd[3];
        
        if (type == COLUMN) {
            if (action == INSTALL) {
                // 기둥 설치
                columns[x][y] = true;
                if (!checkAll()) columns[x][y] = false;
                
            } else {
                // 기둥 삭제
                columns[x][y] = false;
                if (!checkAll()) columns[x][y] = true;
            }
        } else {
            if (action == INSTALL) {
                // 보 설치
                beams[x][y] = true;
                if (!checkAll()) beams[x][y] = false;
            } else {
                // 보 삭제
                beams[x][y] = false;
                if (!checkAll()) beams[x][y] = true;
            }
        }
    }
    
    vector<vector<int>> answer;
    
    // 결과 수집
    for (int x = 0; x <= n; x++) {
        for (int y = 0; y <= n; y++) {
            if (columns[x][y]) answer.push_back({x, y, COLUMN});
            if (beams[x][y]) answer.push_back({x, y, BEAM});
        }
    }
    
    return answer;
}
```

## 다시 풀었지만 오래걸리고 틀렸던 풀이
```java
import java.util.*;

class Solution {
    
    class Frame implements Comparable<Frame>{
        int x, y, type;
        
        Frame(int x, int y, int type){
            this.x = x;
            this.y = y;
            this.type = type;
        }
        
        public int compareTo(Frame f){
            if(this.x == f.x){
                if(this.y == f.y){
                    return this.type - f.type; // 기둥이 먼저
                }
                return this.y - f.y; // y 좌표가 작은 것 먼저
            }
            return this.x - f.x; // x 좌표가 작은 것 먼저
        }
    }
    
    static final int MAX_N = 100;
    static final int COLUMN = 0; // 기둥
    static final int BEAM = 1; // 보
    static final int DELETE = 0;
    static final int INSTALL = 1;
    static final Set<Frame> frames = new TreeSet<>();
    static final boolean[][][] map = new boolean[MAX_N + 1][MAX_N + 1][2]; // y, x, 기둥인지 보인지
    
    public int[][] solution(int n, int[][] build_frames) {
        for(int[] build_frame : build_frames){
            int x = build_frame[0];
            int y = build_frame[1];
            int type = build_frame[2];
            int command = build_frame[3];
            
            if(type == COLUMN && command == INSTALL){
                if(canInstallColumn(y, x)){
                    frames.add(new Frame(x, y, COLUMN));       
                }
            }else if (type == COLUMN && command == DELETE){
                if(canDeleteColumn(y, x)){
                    frames.remove(new Frame(x, y, COLUMN));       
                }
            }else if (type == BEAM && command == INSTALL){
                if(canInstallBeam(y, x)){
                    frames.add(new Frame(x, y, BEAM));
                }
            }else if (type == BEAM && command == DELETE){
                if(canDeleteBeam(y, x)){
                    frames.remove(new Frame(x, y, BEAM));  
                }
            }
        }
        
        int[][] answer = new int[frames.size()][3];
        int idx = 0;
        for(Frame f : frames){
            answer[idx][0] = f.x;
            answer[idx][1] = f.y;
            answer[idx][2] = f.type;
            idx++;
        }
        
        return answer;
    }
    
    boolean canInstallColumn(int y, int x){
        map[y][x][COLUMN] = true;
        if(isStableColumn(y, x)) return true;
        else map[y][x][COLUMN] = false;    
        return false;
    }
    
    boolean canInstallBeam(int y, int x){
        map[y][x][BEAM] = true;
        if(isStableBeam(y, x)) return true;
        else map[y][x][BEAM] = false;    
        return false;
    }
    
    boolean canDeleteColumn(int y, int x){
        map[y][x][COLUMN] = false;
        
        // 윗 기둥이 불안정하면 false
        if(!isStableColumn(y + 1, x)){
            map[y][x][COLUMN] = true;
            return false;  
        } 
        
        // 윗 오른쪽 보가 불안정하면 false
        if(!isStableBeam(y + 1, x)){
            map[y][x][COLUMN] = true;
            return false;  
        } 
        
        // 윗 왼쪽 보가 불안정하면 false
        if(x > 0 && !isStableBeam(y + 1, x - 1)){
            map[y][x][COLUMN] = true;
            return false;  
        } 
        
        return true;
    }
    
    boolean canDeleteBeam(int y, int x){
        map[y][x][BEAM] = false;
        
        // 윗 왼쪽 기둥이 불안정하면 false
        if(!isStableColumn(y, x)){
            map[y][x][BEAM] = true;
            return false;  
        } 
        
        // 윗 오른쪽 기둥이 불안정하면 false
        if(!isStableColumn(y, x + 1)){
            map[y][x][BEAM] = true;
            return false;  
        } 
        
        // 오른쪽 보가 불안정하면 false
        if(!isStableBeam(y, x + 1)){
            map[y][x][BEAM] = true;
            return false;  
        } 
        
        // 왼쪽 보가 불안정하면 false
        if(x > 0 && !isStableBeam(y, x - 1)){
            map[y][x][BEAM] = true;
            return false;  
        } 
        
        return true;
    }
    
    boolean isStableColumn(int y, int x){
        if(!map[y][x][COLUMN]) return true;
        
        // 기둥은 바닥 위에 있거나
        if(y == 0) return true;
        
        // 보의 왼쪽 끝 부분 위에 있거나, 
        if(map[y][x][BEAM]) return true;
        // 보의 오른쪽 끝 부분 위에 있거나, 
        if(x > 0 && map[y][x - 1][BEAM]) return true;
            
        // 또는 다른 기둥 위에 있어야 합니다.
        if(map[y - 1][x][COLUMN]) return true;
        
        return false;
    }
    
    boolean isStableBeam(int y, int x){
        if(!map[y][x][BEAM]) return true;
        
        // 보는 바닥에 있을 수 없다.
        if(y == 0) return false;
        
        // 보는 한쪽 끝 부분이 기둥 위에 있거나
        if(map[y - 1][x][COLUMN] || map[y - 1][x + 1][COLUMN]) return true;
        
        // 또는 양쪽 끝 부분이 다른 보와 동시에 연결되어 있어야 합니다.
        if(x > 0 && map[y][x - 1][BEAM] && map[y][x + 1][BEAM]) return true;
        
        return false;
    }
}
```
isStableBeam에서 `if(!map[y][x][BEAM]) return true;`이걸 꼭 해줘야 하는데 안해줬음...

