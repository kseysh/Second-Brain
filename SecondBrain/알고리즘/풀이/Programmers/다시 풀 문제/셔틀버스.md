# 다른 사람의 풀이
```cpp
#include <string>
#include <vector>
#include <algorithm>
#include <iostream>

using namespace std;

string toTime(int minute) {
    string time = "";
    int hour = minute / 60;
    int m = minute % 60;

    if (hour < 10) {
        time += "0";
    }

    time += to_string(hour);
    time += ":";

    if (m < 10) {
        time += "0";
    }

    time += to_string(m);

    return time;
}

int toMinute(string& a) {
    int a_hour = (a[0] - '0') * 10 + (a[1] - '0');
    int a_minute = (a[3] - '0') * 10 + (a[4] - '0');
    return a_hour * 60 + a_minute;
}

bool Comp(string& a, string& b) {
    return toMinute(a) < toMinute(b);
}

string solution(int n, int t, int m, vector<string> timetable) {
    sort(timetable.begin(), timetable.end(), Comp);

    int shuttle_time = 9 * 60;
    bool is_full = false;
    int answer;
    int shuttle_cnt;
    int people;

    for (shuttle_cnt = 0; shuttle_cnt < n; shuttle_cnt++, shuttle_time += t) {
        for (people = 0; people < m; people++) { // 각 셔틀마다 m명씩 태움
            if (timetable.empty()) break; // 더 이상 탈 사람이 없으면 break
            if (toMinute(timetable.front()) > shuttle_time) break; // 셔틀 시간 이후에 탈 수는 없음
            answer = toMinute(timetable.front()); // 탑승 가능한 사람 중 가장 앞 사람을 태움
            timetable.erase(timetable.begin()); // 지나간 시간은 없애버림
        }
    }

    shuttle_time -= t; // for문에서 마지막에 t를 더하고 끝내버리기 때문에 빼준 것, 콘이 타야하니까

    if (shuttle_cnt == n && people == m) { //막차에 사람이 꽉 찼을 경우 마지막에 탄 사람보다 일찍 와야 함.
        return toTime(answer - 1);
    } else { //막차에 자리에 있을 경우 셔틀이 출발할 때 오면 됨.
        return toTime(shuttle_time);
    }
}
```
기교없는 진짜 구현 문제인데... 이해가 어려웠다 이런 구현 문제 잘 풀 수 있도록 연습하자