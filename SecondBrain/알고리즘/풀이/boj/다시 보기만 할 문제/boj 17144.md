순환하는 코드에 대해서만 한 번 보자
![[Pasted image 20260106095945.png|300]]


## 내 풀이
```java
static void doClean(){
        // 반시계 회전
        for (int y = rccy - 2; y >= 0; y--) {
            map[y + 1][0] = map[y][0];
        }

        for (int x = 1; x < c; x++) {
            map[0][x - 1] = map[0][x];
        }

        for (int y = 1; y <= rccy; y++) {
            map[y - 1][c - 1] = map[y][c - 1];
        }

        for (int x = c - 1; x >= 0; x--) {
            map[rccy][x + 1] = map[rccy][x];
        }

        map[rccy][1] = 0;

        // 시계 회전
        for (int y = ccy + 2; y < r; y++) {
            map[y - 1][0] = map[y][0];
        }

        for (int x = 1; x < c; x++) {
            map[r - 1][x - 1] = map[r - 1][x];
        }

        for (int y = r; y >= ccy; y--) {
            map[y][c - 1] = map[y - 1][c - 1];
        }

        for (int x = c - 1; x >= 0; x--) {
            map[ccy][x + 1] = map[ccy][x];
        }

        map[ccy][1] = 0;
    }
```

## 다른 사람의 풀이
```java
static void airCirculation() {
		int a = 0, b = 0; //swap 위한 변수. a:이전값 b:현재값 임시저장
		//위 반시계 방향 회전 0->1->2->3
		int row = r1, col = c1;
		for(int i=0; i<4; ++i) {
			while(true) {
				row += dx[i];
				col += dy[i];
				if(row<0||row==R||col<0||col==C||map[row][col]==-1) {
					row -= dx[i];
					col -= dy[i];
					break;
				}
				b = map[row][col];
				map[row][col] = a;
				a = b;
			}
		}
		total -= a;
		//아래 시계방향 회전  0->3->2->1
		a = b = 0;
		row = r1+1;
		col = c1;
		for(int i=4; i>=1; --i) {
			int j = i%4;
			while(true) {
				row += dx[j];
				col += dy[j];
				if(row<0||row==R||col<0||col==C||map[row][col]==-1) {
					row -= dx[j];
					col -= dy[j];
					break;
				}
				b = map[row][col];
				map[row][col] = a;
				a = b;
			}
		}
		total -= a;
	}
```
명확하다고 느껴지지는 않는다...