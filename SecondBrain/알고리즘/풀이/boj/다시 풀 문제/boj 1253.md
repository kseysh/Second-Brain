```java
import java.io.*;
import java.util.*;

public class Main {
    public static int n;
    public static Integer answer;
    public static List<Long> list = new ArrayList<>();

    public static void main(String[] args) throws IOException{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        n = Integer.parseInt(br.readLine());
        StringTokenizer st = new StringTokenizer(br.readLine());
        for (int i = 0; i < n; i++) {
            list.add(Long.parseLong(st.nextToken()));
        }

        list.sort(Comparator.naturalOrder());
        answer = 0;

        for (int result = n - 1; result >= 0 ; result--) {
            long r = list.get(result);
            int start = 0;
            int end = n - 1;
            while(start < end){
                long s = list.get(start);
                long e = list.get(end);

                if(s + e < r){
                    start++;
                }else if(s + e > r){
                    end--;
                }else{
                    if (start != result && end != result){
                        answer++;
                        break;
                    } else if (start == result) {
                        start++;
                    } else {
                        end--;
                    }
                }
            }
        }

        BufferedWriter bw = new BufferedWriter(new OutputStreamWriter(System.out));
        bw.write(answer.toString());
        bw.close();
    }
}

```
예외 케이스를 제대로 컨트롤하지 못했습니다.