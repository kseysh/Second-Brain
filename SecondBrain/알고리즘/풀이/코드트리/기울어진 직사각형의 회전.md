```java
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[][] grid = new int[n + 1][n + 1];
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                grid[i][j] = sc.nextInt();
        int r = sc.nextInt();
        int c = sc.nextInt();
        int m1 = sc.nextInt();
        int m2 = sc.nextInt();
        int m3 = sc.nextInt();
        int m4 = sc.nextInt();
        int dir = sc.nextInt();
        
        int temp = grid[r][c];
        int nn, mm;
        if(dir == 0){
            nn = m4;
            mm = m3;
        }else{
            nn = m3;
            mm = m4;
        }
        for(int i = 0; i < nn; i++){
            if(dir == 0){
                grid[r][c] = grid[r - 1][c - 1];
                r--; c--;
            }else{
                grid[r][c] = grid[r - 1][c + 1];
                r--; c++;
            }
        }

        for(int i = 0; i < mm; i++){
            if(dir == 0){
                grid[r][c] = grid[r - 1][1+c];
                r--; c++;
            }else{
                grid[r][c] = grid[r - 1][c - 1];
                r--; c--;
            }
        }

        for(int i = 0; i < nn; i++){
            if(dir == 0){
                grid[r][c] = grid[1+r][1+c];
                r++; c++;
            }else{
                grid[r][c] = grid[r + 1][c - 1];
                r++; c--;
            }
        }

        for(int i = 0; i < mm; i++){
            if(dir == 0){
                grid[r][c] = grid[1+r][c-1];
                r++; c--;
            }else{
                grid[r][c] = grid[r + 1][c + 1];
                r++; c++;
            }
            
        }
        if(dir == 0){
            grid[r - 1][c + 1] = temp;
        }else{
            grid[r - 1][c - 1] = temp;
        }
        

        for (int i = 1; i <= n; i++){
            for (int j = 1; j <= n; j++){
                System.out.print(grid[i][j] + " ");
            }
            System.out.println();
        }       
    }
}
```

풀이는 구했지만, 한 번 틀렸고, 풀이가 더럽다.
dy dx 테크닉과 moveNum을 활용해서 풀이할 수 있도록 다시 한 번 풀자

```java
import java.util.Scanner;
import java.util.Arrays;
import java.util.ArrayList;

public class Main {
    public static final int CW = 1;
    public static final int CCW = 0;
    public static final int DIR_NUM = 4;
    public static final int MAX_N = 100;
    
    public static int n;
    public static int[][] grid = new int[MAX_N][MAX_N];
    public static int[][] temp = new int[MAX_N][MAX_N];
    
    public static void shift(int x, int y, int k, int l, int moveDir) {
        int[] dx;
        int[] dy;
        int[] moveNums;
        
        if(moveDir == CCW) {
            dx = new int[]{-1, -1, 1, 1};
            dy = new int[]{1, -1, -1, 1};
            moveNums = new int[]{k, l, k, l}; 
        }
        else {
            dx = new int[]{-1, -1, 1, 1};
            dy = new int[]{-1, 1, 1, -1};
            moveNums = new int[]{l, k, l, k};
        }
        
        // Step1. temp 배열에 grid 값을 복사합니다.
        for(int i = 0; i < n; i++)
            for(int j = 0; j < n; j++)
                temp[i][j] = grid[i][j];
    
        // Step2. 기울어진 직사각형의 경계를 쭉 따라가면서
        //        숫자를 한 칸씩 밀었을 때의 결과를
        //        temp에 저장합니다.
        for(int d = 0; d < DIR_NUM; d++)
            for(int q = 0; q < moveNums[d]; q++) {
                int nx = x + dx[d], ny = y + dy[d];
                temp[nx][ny] = grid[x][y];
                x = nx; y = ny;
            }
        
        // Step3. temp값을 grid에 옮겨줍니다.
        for(int i = 0; i < n; i++)
            for(int j = 0; j < n; j++)
                grid[i][j] = temp[i][j];
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        n = sc.nextInt();
        
        for(int i = 0; i < n; i++)
            for(int j = 0; j < n; j++) 
                grid[i][j] = sc.nextInt();
        
        int x, y, m1, m2, m3, m4, d;
        x = sc.nextInt();
        y = sc.nextInt();
        m1 = sc.nextInt();
        m2 = sc.nextInt();
        m3 = sc.nextInt();
        m4 = sc.nextInt();
        d = sc.nextInt();
        
        shift(x - 1, y - 1, m1, m2, d);
        
        for(int i = 0; i < n; i++) {
            for(int j = 0; j < n; j++)
                System.out.print(grid[i][j] + " ");
            System.out.println();
        }
    }
}

```